/**
 * Cửa vào của app cho mã hoá mã link (BB-201) — chốt `server-only` để không
 * bao giờ lọt vào mã trình duyệt. Thân nằm ở `ma-link-loi.ts`.
 */
import "server-only";
export { maHoaMaLink, giaiMaMaLink } from "./ma-link-loi";
